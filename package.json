{
  "license": "MIT",
  "name": "up-pocketsmith-lambda",
  "scripts": {
    "build": "tsc",
    "deploy:develop": "yarn build && sls deploy --stage develop",
    "deploy:production": "yarn build && sls deploy --stage production"
  },
  "dependencies": {
    "axios": "^0.21.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.64",
    "@types/axios": "^0.14.0",
    "@types/node": "^14.14.6",
    "typescript": "^4.0.5"
  }
}
